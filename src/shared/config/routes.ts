export const routes = {
  root: '/',
  cards: '/cards',
  plan: '/plan',
  builder: '/builder',
  game: '/game',
  allWords: '/all-words',
  learned: '/learned',
  unlearned: '/unlearned',
} as const
